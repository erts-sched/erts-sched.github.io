title: Guilherme Silva
description: Trust boundaries of the BEAM runtime, measured
permalink: pretty
markdown: kramdown
defaults:
  - scope:
      path: ""
    values:
      layout: default
exclude:
  - README.md
